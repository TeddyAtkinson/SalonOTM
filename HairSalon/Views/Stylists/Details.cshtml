@{
Layout = "_Layout";
}

@model Salon.Models.Stylist
@using Salon.Models


<h2>Stylist Details</h2>

<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)<h3>

@if (@Model.Clients.Count == 0)
{
<h3>No Clients have been added for @Model.Name</h3>
}
else
{
<h3>Clients:</h3>
<ul class="row">
@foreach (Client client in Model.Clients)
{
<li id="client">@Html.ActionLink($"{client.Name}", "Details", "Clients", new { id = client.ClientId })</li>
}
</ul>
}

<p>@Html.ActionLink("Edit Stylist", "Edit", new { id = Model.StylistId })</p>
<p>@Html.ActionLink("Delete Stylist", "Delete", new { id = Model.StylistId })</p>
<p>@Html.ActionLink("Add new Client", "Create", "Clients")</p>
<p>@Html.ActionLink("Back to list", "Index")</p>
